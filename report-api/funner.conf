root = "."  # Project root directory
build_delay = 500ms  # Delay before the command is triggered
build_args = ["-o", "tmp/main"]  # Build command arguments
colors = true  # Enable colorized output
log_color_main = "cyan"  # Color for main logs
log_color_build = "yellow"  # Color for build logs
